apiVersion: v1
kind: Service
metadata:
  name: {{ include "student-stack.fullname" . }}
  labels:
    {{- include "student-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "student-stack.selectorLabels" . | nindent 4 }}
