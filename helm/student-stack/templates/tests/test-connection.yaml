apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "student-stack.fullname" . }}-test-connection"
  labels:
    {{- include "student-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "student-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
