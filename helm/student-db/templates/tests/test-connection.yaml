apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "student-db.fullname" . }}-test-connection"
  labels:
    {{- include "student-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "student-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
